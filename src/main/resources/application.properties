# Sathya Agencies Application Defaults (Overridable via .env or Environment Variables)
app.name=Sathya Agencies
app.version=1.0.0

# Database Defaults
db.host=localhost
db.port=3306
db.name=pharmacy_erp
db.user=root
db.password=
db.pool.size=10

# Security Defaults
app.allow.demo.login=false
